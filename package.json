{
  "name": "webtoapp-builder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/configure.js && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/cli": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  }
}
